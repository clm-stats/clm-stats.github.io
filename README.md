this repo just holds the latest build of the frontend and the json file database in jekyll file format structure. you could not include build artifacts in the repo and use github actions to build but they start to make you pay for that eventually.

for the json file db generating: https://github.com/clm-stats/clm-stats.manager

for the frontend: https://github.com/clm-stats/clm-stats.app

